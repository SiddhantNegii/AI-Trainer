# API Base URL (local dev fallback)
NEXT_PUBLIC_API_URL=http://localhost:8001

# Render Blueprint provided values (preferred in production)
# These are automatically injected by render.yaml; do not change.
NEXT_PUBLIC_API_HOST=
NEXT_PUBLIC_API_HOSTPORT=
